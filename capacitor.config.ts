import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.josefgrasberger.bouldertracker',
  appName: 'Boulder Tracker',
  webDir: 'dist'
};

export default config;
